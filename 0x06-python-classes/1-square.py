#!/usr/bin/python3
"""defines a square using the variables size"""
class Square:
    """calculates a square"""
    def __init__(self, size):
    """initializes a square

        Arg:
            size(int) the size of a square"""
        self.__size = size
