#!/bin/bash
#A Bash script that takes in a URL, sends a request to that URL, and displays the size of the body of the response
curl http://www.nowere.com:500/docs/test.txt(\n.s.stat('/docs/test.txt'))
